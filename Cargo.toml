[package]
name = "account-analyzer"
version = "0.1.0"
edition = "2024"
authors = ["Chetan Khilosiya <chetan.khilosiya@gmail.com>"]
description = "Application to analyze the bank statements and get data based on queries"
license = "MIT"
keywords = ["bank", "account", "analyze", "statement"]
rust-version = "1.95.0"

[dependencies]
dioxus = { version = "0.7.10", features = ["router"] }
calamine = { version = "0.35.0", features = ["chrono"]}
anyhow = "1.0.104"
chrono = { version = "0.4.45", features = ["std", "serde"]}
dioxus-primitives = { git = "https://github.com/DioxusLabs/components", version = "0.0.1", default-features = false }
dioxus-tabular = "0.3.0"

[features]
default = ["desktop"]
# The feature that are only required for the web = ["dioxus/web"] build target should be optional and only enabled in the web = ["dioxus/web"] feature
web = ["dioxus/web"]
# The feature that are only required for the desktop = ["dioxus/desktop"] build target should be optional and only enabled in the desktop = ["dioxus/desktop"] feature
desktop = ["dioxus/desktop"]
# The feature that are only required for the mobile = ["dioxus/mobile"] build target should be optional and only enabled in the mobile = ["dioxus/mobile"] feature
mobile = ["dioxus/mobile"]
